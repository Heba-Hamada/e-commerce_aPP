import 'package:flutter/material.dart';

class FavierateScr extends StatelessWidget {
  const FavierateScr({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.yellow,
      ),
    );
  }
}
